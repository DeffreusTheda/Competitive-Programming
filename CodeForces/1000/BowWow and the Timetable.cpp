#include <iostream>
#include <vector>
#include <bitset>
using namespace std;

#define ll long long

string s;
ll id;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    // for(int n=1; n<=10000; ++n) {
    //     for(int j=1; j<=n; ++j) {
    //         ++id;
    //     }
    // }
    // cout<<id/5050; //4950


    return 0;
}
