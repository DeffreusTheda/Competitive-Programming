#include <iostream>
#include <vector>

using namespace std;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int n;
    cin>>n;
    int str[n+5];
    for(int i=1; i<=n; ++i) {
        cin>>str[i];
    }
    bool b[n];
    for(int i=1; i<=n; ++i) {
        for(int j=1; j<=n; ++j) {

        }
    }

    return 0;
}
